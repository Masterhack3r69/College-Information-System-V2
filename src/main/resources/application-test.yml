spring:
  datasource:
    url: jdbc:h2:mem:sis;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

sis:
  jwt:
    secret: test_secret_test_secret_test_secret_test_secret_test_secret
    access-expiration-seconds: 900
    refresh-expiration-seconds: 604800

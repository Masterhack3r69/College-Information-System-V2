package com.school.sis.grade.repository; import com.school.sis.grade.entity.*; import org.springframework.data.jpa.repository.*; import java.util.*; public interface ClassGradebookRepository extends JpaRepository<ClassGradebook,UUID>{Optional<ClassGradebook> findByScheduleId(UUID scheduleId);boolean existsByTemplateId(UUID templateId);boolean existsByScaleId(UUID scaleId);List<ClassGradebook> findByStatus(GradeStatus status);}

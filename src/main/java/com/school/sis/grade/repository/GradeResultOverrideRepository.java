package com.school.sis.grade.repository; import com.school.sis.grade.entity.GradeResultOverride; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface GradeResultOverrideRepository extends JpaRepository<GradeResultOverride,UUID>{List<GradeResultOverride> findByGradebookId(UUID id);Optional<GradeResultOverride> findByGradebookIdAndEnrollmentSubjectId(UUID g,UUID s);}

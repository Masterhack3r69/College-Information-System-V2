package com.school.sis.grade.repository; import com.school.sis.grade.entity.GradeScore; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface GradeScoreRepository extends JpaRepository<GradeScore,UUID>{List<GradeScore> findByItemGradebookId(UUID id);Optional<GradeScore> findByItemIdAndEnrollmentSubjectId(UUID itemId,UUID subjectId);void deleteByItemId(UUID itemId);}

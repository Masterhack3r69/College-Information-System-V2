import { Link } from "react-router-dom"
import { BookOpen, Search } from "lucide-react"
import { useState } from "react"
import { useFacultyClasses } from "@/hooks/use-faculty-portal"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from "@/components/ui/table"
export default function FacultyClassesPage(){const q=useFacultyClasses(),[search,setSearch]=useState("");const rows=(q.data??[]).filter(x=>`${x.courseCode} ${x.courseTitle} ${x.sectionCode}`.toLowerCase().includes(search.toLowerCase()));return <div className="mx-auto max-w-[1240px] p-4 sm:p-6 lg:p-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 className="text-2xl font-semibold tracking-tight text-foreground sm:text-[1.75rem]">My Classes</h1><p className="mt-1 text-muted-foreground">Assigned subjects and sections for the active term.</p></div><div className="relative w-full sm:w-72"><Search className="absolute left-3 top-2.5 text-muted-foreground"/><Input className="pl-9" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search classes"/></div></div><div className="mt-7 overflow-hidden rounded-lg border"><Table><TableHeader><TableRow><TableHead>Class</TableHead><TableHead>Term</TableHead><TableHead>Room</TableHead><TableHead>Students</TableHead><TableHead>Gradebook</TableHead><TableHead/></TableRow></TableHeader><TableBody>{rows.map(x=><TableRow key={x.scheduleId}><TableCell><p className="font-semibold">{x.courseCode} · {x.sectionCode}</p><p className="text-xs text-muted-foreground">{x.courseTitle}</p></TableCell><TableCell>{x.semesterName.replaceAll("_"," ")}<span className="block text-xs text-muted-foreground">AY {x.schoolYear}</span></TableCell><TableCell>{x.roomCode}</TableCell><TableCell>{x.studentCount}</TableCell><TableCell>{x.gradeStatus.replaceAll("_"," ")}</TableCell><TableCell className="text-right"><Button asChild variant="outline"><Link to={`/faculty/classes/${x.scheduleId}`}><BookOpen data-icon="inline-start"/>Open class</Link></Button></TableCell></TableRow>)}</TableBody></Table></div></div>}

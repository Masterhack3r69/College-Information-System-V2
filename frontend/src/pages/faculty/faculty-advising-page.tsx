import { useState } from "react"
import { Search,UsersRound } from "lucide-react"
import { useAdvisees } from "@/hooks/use-faculty-portal"
import { Input } from "@/components/ui/input"
import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from "@/components/ui/table"
export default function FacultyAdvisingPage(){const q=useAdvisees(),[search,setSearch]=useState("");const rows=(q.data??[]).filter(x=>`${x.studentName} ${x.studentNumber}`.toLowerCase().includes(search.toLowerCase()));return <div className="mx-auto max-w-[1200px] p-5 md:p-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 className="flex items-center gap-3 text-3xl font-semibold text-[#092f66]"><UsersRound/>Advising</h1><p className="mt-1 text-slate-600">Assigned advisees, academic standing, and contact basics.</p></div><div className="relative w-full sm:w-72"><Search className="absolute left-3 top-2.5 text-slate-400"/><Input className="pl-9" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search advisees"/></div></div><div className="mt-7 overflow-hidden rounded-lg border"><Table><TableHeader><TableRow><TableHead>Student</TableHead><TableHead>Program</TableHead><TableHead>Section</TableHead><TableHead>Year</TableHead><TableHead>Standing</TableHead><TableHead>Contact</TableHead></TableRow></TableHeader><TableBody>{rows.map(x=><TableRow key={x.studentId}><TableCell><p className="font-medium">{x.studentName}</p><p className="text-xs text-slate-500">{x.studentNumber}</p></TableCell><TableCell>{x.programCode}</TableCell><TableCell>{x.sectionCode}</TableCell><TableCell>{x.yearLevel}</TableCell><TableCell>{x.academicStatus?.replaceAll("_"," ")}</TableCell><TableCell><p>{x.email}</p><p className="text-xs text-slate-500">{x.mobileNumber}</p></TableCell></TableRow>)}</TableBody></Table></div></div>}
